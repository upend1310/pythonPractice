# Tuples are similar to lists but immutable
coordinates = (4, 5)
print(coordinates[0])
# coordinates[1] = 10
# we can crate a list of tuples
new_coordinates = [(4, 5), (6, 7)]
print(new_coordinates)


