import usefulTools

print(usefulTools.roll_dice(10))

# We can use pip to install external modules
# there are some build in modules which are present in Lib
# for 3rd party modules use pip and then can be seen under site-packages
